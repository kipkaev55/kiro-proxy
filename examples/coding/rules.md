# Coding Rules

When writing or reviewing code:

- Prefer small, focused functions over large blocks.
- Include error handling for external calls (network, filesystem, subprocess).
- Match the existing style of the file you are editing.
- Do not introduce new dependencies unless asked.
- When editing an existing file, show only the changed parts unless a full file is requested.
- Before claiming work is done, describe how the user can verify it.
